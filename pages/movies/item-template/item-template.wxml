<import src="../star-template/star-template.wxml" />
<template name="item">
    <view class="item-container" j>
        <image class="item-img" src="{{elem.url}}" />
        <text>{{elem.title}}</text>
        <template is="star" data="{{elem}}" />
    </view>
</template>