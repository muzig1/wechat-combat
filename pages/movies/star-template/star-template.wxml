<template name="star">
    <view class="container">
        <block wx:key="item" wx:for="{{items}}" wx:for-item="item">
            <image class="star-img" src="{{item}}" />
        </block>
        <text class="star-text">8.7</text>
    </view>
</template>